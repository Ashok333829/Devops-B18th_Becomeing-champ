# Devops-B18th_Becomeing-champ
This central repository has created to practice all Devops activities for showcase in publicway. ( Workspace )

=======================================================================================================================================:
 
- In this repo ishould practice what i learn about Git and Github 

Branching,
Move file in3 ways to central,
backword the file by doing Reset,
Stashing the files,
Tags,
permissions - chmod ,chown, chgrp .

Finally push this all to cental repo github.

All linux commands practice.


git commit --amend -m "message".
git rebase --all
git pull --rebase
git fetch
git push origin/main
git push origin <branch.name>

Branching commands :

1. Git branch
2. git branch <b.name>
3. git checkout <b.name>
4. git switch <b.name>
5. git branch -D <b.name>
6. git branch -r
7. git branch -a
8. git branch -v



Reset commands :

git add.
git commit -m "message"

1. git reset --soft <commit.id> n-1,
2. git reset HEAD <commit.id> select purticular f.name, 
3. git reset --mixed <commit.id> n-1.

stash commands :

1. git stash
2. git stash list
3. git stash save <f.name>
4. git stash apply
5. git stash pop
6. git stash drop

Tags commands :

1. git tag
2. git tag list
3. git tag "file.name"
4. git tag -a "file.name" -m "message"
5. git tag -a "file.name" -f "file.name" -m "messgae" -> Modefied the Tags.
6. git tag -r <t.name>

=======================================================================================================================================:

Thank you,
Ashok T.
